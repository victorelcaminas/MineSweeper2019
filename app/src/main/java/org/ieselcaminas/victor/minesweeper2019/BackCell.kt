package org.ieselcaminas.victor.minesweeper2019

import android.content.Context
import android.widget.ImageView

class BackCell(number: Int, context: Context): ImageView(context) {
    init {
        when(number) {
            1-> drawable = getI
        }
    }
}